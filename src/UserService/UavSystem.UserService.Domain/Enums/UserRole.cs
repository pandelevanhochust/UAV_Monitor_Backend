namespace UavSystem.UserService.Domain.Enums;

/// <summary>
/// Authorization roles enforced across the UAV system.
/// Domain-owned enum — zero external dependencies.
/// </summary>
public enum UserRole
{
    Admin,
    Monitor
}
